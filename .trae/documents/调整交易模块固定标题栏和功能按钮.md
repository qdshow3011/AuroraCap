1. **修改TradesScreen.tsx的根布局**：将ScrollView从根容器改为只包裹内容区域，根容器使用普通View
2. **固定标题栏**：将mainHeader移到ScrollView外部，使其在内容滚动时保持不动
3. **固定功能按钮区域**：将tradeButtons移到ScrollView外部，使其在内容滚动时保持不动
4. **调整样式**：确保标题栏和功能按钮区域的样式与赎回申请页面一致，包括间距、阴影等
5. **优化内容区域**：将renderMainView等返回的内容包裹在ScrollView中，只让内容部分滚动
6. **保持其他视图不变**：确保其他子视图（如申购、赎回等）的布局不受影响

**修改步骤**：

1. 修改return语句的根布局结构
2. 将mainHeader和tradeButtons移到ScrollView外部
3. 调整ScrollView的样式，使其只包裹内容区域
4. 确保功能按钮区域只在main视图显示
5. 测试布局效果，确保标题栏和功能按钮固定，内容可以正常滚动

