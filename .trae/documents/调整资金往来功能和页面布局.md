# 调整资金往来功能和页面布局

## 1. 首页资金往来按钮链接调整
- **文件**：`PortfolioScreen.tsx`
- **调整内容**：确保首页的"资金往来"按钮正确链接到`FundTransactionsScreen`页面
- **当前状态**：按钮已存在，需要确保导航逻辑正确

## 2. 资管页面功能按钮调整
- **文件**：`ProductsScreen.tsx`
- **调整内容**：
  - 将"成交状况"按钮改为"资金往来"
  - 图标从✅改为💹
  - 将onPress事件链接到`FundTransactionsScreen`页面
- **位置**：资管页面顶部四个功能按钮中的第四个

## 3. 资金往来页面调整
- **文件**：`FundTransactionsScreen.tsx`
- **调整内容**：
  - 添加返回键
  - 调整布局顺序：将"年度资金往来"图表移到"全部记录"列表上方
  - "全部记录"放到页面最下面

## 4. 导航逻辑完善
- **文件**：`App.tsx`
- **调整内容**：确保`onNavigateTo`处理函数正确处理'fundTransactions'导航请求

## 具体实现步骤

1. **修改资管页面的功能按钮**（ProductsScreen.tsx）：
   - 将"成交状况"按钮文本改为"资金往来"
   - 更换图标为💹
   - 添加`onNavigateToFundTransactions`参数调用

2. **调整资金往来页面布局**（FundTransactionsScreen.tsx）：
   - 在header中添加返回按钮，需要接收onClose参数
   - 交换"全部记录"和"年度资金往来"的位置

3. **完善App.tsx中的导航逻辑**：
   - 在PortfolioScreen的onNavigateTo处理函数中添加'fundTransactions'的处理
   - 确保从ProductsScreen可以正确导航到FundTransactionsScreen

4. **测试所有导航链接**：
   - 首页资金往来按钮
   - 资管页面资金往来按钮
   - 资金往来页面返回按钮
   - 资金往来页面内部的功能按钮

通过以上调整，将实现用户要求的所有功能和布局变更。