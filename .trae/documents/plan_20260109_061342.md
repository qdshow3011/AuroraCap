### 资管模块调整计划

根据用户要求，我将对资管模块进行以下调整：

#### 1. 将"我的资产"改为"我的资产状况"
- **修改翻译文本**：更新 `t.myAssets` 的翻译值
- **更新所有引用**：确保所有显示"我的资产"的地方都改为"我的资产状况"
  - 资产状况页面标题
  - 资管页面中的资产区块标题
  - 相关按钮文字

#### 2. 为基金产品添加可售状态字段
- **修改产品渲染逻辑**：在每个基金产品信息中添加"可售状态"显示
- **数据来源**：从数据库 `products` 表中获取 `sale_status` 字段
- **显示内容**：
  - 如果 `sale_status` 为 `open`，显示"已开放"
  - 如果 `sale_status` 为 `closed`，显示"已售净"
  - 其他情况显示"未知状态"
- **样式设计**：使用不同颜色区分不同状态

#### 3. 实现细节

**修改翻译对象**：
- 在翻译对象中添加 `saleStatus`、`openSale`、`closedSale` 等相关翻译

**更新产品渲染**：
- 在产品详情中添加可售状态显示
- 使用 `detailItem` 样式保持与其他详情项一致
- 根据状态值设置不同的文字颜色

**数据库字段处理**：
- 确保 `products` 表中存在 `sale_status` 字段
- 支持两种状态值：`open`（已开放）和 `closed`（已售净）
- 处理字段不存在的情况，提供默认值

#### 4. 预期效果

调整后，资管模块将：
- 统一显示"我的资产状况"而不是"我的资产"
- 每个基金产品都显示其可售状态
- 状态文字颜色区分，便于用户识别
- 布局保持美观，与整体设计协调

#### 5. 测试与优化
- 测试不同状态下的显示效果
- 确保数据库字段不存在时不会崩溃
- 优化状态显示的视觉效果

### 实现步骤

1. 修改翻译文本
2. 更新资产区块标题
3. 为基金产品添加可售状态显示
4. 添加相关样式
5. 测试不同状态下的显示效果