## 实现申请成功页面

### 1. 创建申请成功页面组件

* **文件路径**: `apps/mobile/src/screens/SubscriptionSuccessScreen.tsx`

* **功能**: 显示申请成功信息，并从数据库获取最新提交的数据

* **设计**:

  * 顶部导航栏，包含返回按钮

  * 成功状态图标和标题

  * 显示最新投资申请记录的详细信息

  * 提供返回主页或查看更多的按钮

### 2. 更新 App.tsx

* **添加状态变量**:

  ```typescript
  const [showSubscriptionSuccess, setShowSubscriptionSuccess] = useState(false);
  const [latestSubscriptionId, setLatestSubscriptionId] = useState<string | null>(null);
  ```

* **更新条件渲染**:

  * 添加申请成功页面的条件渲染

  * 传递必要的props（lang, userInfo, subscriptionId, onClose）

### 3. 修改 SubscriptionApplicationScreen.tsx

* **更新提交逻辑**:

  * 保存新创建的订阅ID

  * 提交成功后设置 `showSubscriptionSuccess` 为 true

  * 传递 `latestSubscriptionId` 给成功页面

* **修改 onClose 回调**:

  * 确保能正确关闭所有相关状态

### 4. 实现数据获取功能

* 在 SubscriptionSuccessScreen 中添加数据加载逻辑

* 使用 supabase 从 subscription\_redemption 表获取最新的订阅记录

* 显示产品名称、金额、份额等关键信息

### 5. 优化用户体验

* 添加加载状态

* 处理数据获取失败的情况

* 提供清晰的导航选项

* 确保界面美观，符合应用风格

### 6. 测试功能

* 确保提交成功后正确跳转

* 验证数据显示准确性

* 测试各种导航路径

### 技术要点

* 使用 React Native 和 Supabase

* 保持与现有代码结构一致

* 遵循现有命名和样式规范

* 确保代码可维护性和可扩展性

