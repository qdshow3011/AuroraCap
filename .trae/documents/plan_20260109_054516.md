### 资管模块调整计划

根据用户要求，我将对资管模块进行以下调整：

#### 1. 我的资产区块调整
- **将"我的资产"文字放到区块内**：
  - 移除 `section` 容器外的 `sectionTitle`
  - 在 `assetsContainer` 内部添加标题
  - 调整标题样式使其与区块内其他元素协调

#### 2. 增加在途资金项
- **修改数据结构**：在 `MyAssets` 接口中添加 `pendingFunds` 字段
- **更新数据获取逻辑**：从 `fundStatus` 中获取 `pendingAmount` 并设置到 `assets` 中
- **添加UI展示**：在现金余额下方添加在途资金显示

#### 3. 取消资金管理区块，优化最近记录
- **删除资金管理区块**：移除 `fundManagement` 相关的所有代码
- **创建独立的资金往来记录区块**：
  - 标题为"资金往来最新记录"，显示在左侧
  - 右侧添加"更多>>"按钮
  - 只显示3条最新记录
  - 调整记录样式，确保美观

#### 4. 优化基金产品布局
- **修改基金产品容器**：将所有基金产品放入一个统一的区块内
- **标题调整**：将"基金产品"标题移到区块内部
- **移除独立区块**：删除每个基金产品的独立卡片样式，改为列表形式

#### 5. 添加申请申购按钮
- **为每个基金产品添加按钮**：
  - 在基金产品信息右侧添加"申请申购"按钮
  - 按钮样式与整体设计协调
  - 添加点击事件处理

### 预期效果

调整后，资管模块将更加紧凑和用户友好：
- 资产信息展示更加清晰
- 资金往来记录独立成区块，便于查看
- 基金产品布局更加简洁，操作更加便捷
- 整体设计更加统一和专业

### 实现步骤

1. 修改 `MyAssets` 接口，添加 `pendingFunds` 字段
2. 更新数据获取逻辑，设置 `pendingFunds` 值
3. 调整"我的资产"区块布局
4. 删除资金管理区块代码
5. 创建新的资金往来记录区块
6. 优化基金产品布局
7. 为每个基金产品添加申购按钮
8. 调整样式，确保整体协调