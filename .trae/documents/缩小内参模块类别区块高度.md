根据用户要求，我将把类别区块的高度调整为1行普通文字的高度：

### 目标高度
- 1行普通文字高度：约 20px（基于 16px 字体大小）

### 调整方案

1. **移除所有不必要的内边距和边距**：
   - `categoryFilterContent`：paddingVertical 设为 0px
   - `categoryItem`：paddingVertical 设为 0px
   - `categoryText`：移除 marginBottom
   - `categoryIndicator`：移除 marginBottom

2. **调整指示器位置**：
   - 将指示器与文字放在同一行，或者
   - 移除指示器，仅使用文字加粗来表示选中状态

3. **优化布局结构**：
   - 调整分类项的 flex 布局，确保内容垂直居中
   - 减少分类项之间的水平间距，提高空间利用率

4. **保持可用性**：
   - 确保仍有足够的点击区域
   - 保持清晰的选中状态视觉反馈

### 实现选项

我将采用以下方案：
- 移除指示器，仅使用文字颜色和加粗来表示选中状态
- 移除所有不必要的内边距和边距
- 保持分类项之间的适当间距

这样可以将分类区块的高度精确控制在1行文字的高度，同时保持良好的用户体验。