**修改目标**：取消资金往来页面内容部分的上下拉动杆（垂直滚动条）

**修改内容**：

1. **添加showsVerticalScrollIndicator属性**：在FlatList组件中添加showsVerticalScrollIndicator={false}属性，用于隐藏垂直滚动条

**修改步骤**：

1. 打开FundTransactionsScreen.tsx文件
2. 在FlatList组件中添加showsVerticalScrollIndicator={false}属性
3. 运行TypeScript检查，确保修改后的代码没有类型错误

**预期效果**：

* 资金往来页面的内容部分不再显示垂直滚动条

* 页面布局和其他功能保持不变

* 用户在滚动页面时看不到滚动指示器

**技术要点**：

* 垂直滚动条是React Native FlatList的默认行为

* 通过设置showsVerticalScrollIndicator={false}可以隐藏垂直滚动条

* 这个属性只影响视觉效果，不影响滚动功能

**修改文件**：

* src/screens/FundTransactionsScreen.tsx

**修改结果**：

* 已成功添加showsVerticalScrollIndicator={false}属性

* 代码通过TypeScript检查，没有类型错误

* 垂直滚动条已被隐藏，实现了用户要求的效果

