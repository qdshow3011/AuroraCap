## 调整客服模块样式

### 1. 调整对话窗口底色
- 将容器背景色从 `#ece5dd` 调整为更浅的 `#f7f7f7`
- 修改文件：`CustomerServiceScreen.tsx`
- 修改位置：`styles.container` 的 `backgroundColor` 属性

### 2. 调整头像样式
- 将圆形头像改为圆角正方体头像
- 修改 `avatar` 样式的 `borderRadius` 从 `18` 改为 `6`
- 保持宽高一致（36x36px）
- 修改文件：`CustomerServiceScreen.tsx`
- 修改位置：`styles.avatar` 的 `borderRadius` 属性

### 3. 调整消息布局
将时间和已读状态从消息气泡内部移到外部：

#### 3.1 修改文本消息布局
- 从 `messageBubble` 组件中移除 `messageTimeContainer`
- 在消息气泡外部添加时间和已读状态显示
- 修改文件：`CustomerServiceScreen.tsx`
- 修改位置：文本消息渲染部分（第640-660行）

#### 3.2 修改语音消息布局
- 从 `voiceMessageBubble` 组件中移除 `messageTimeContainer`
- 在语音消息气泡外部添加时间和已读状态显示
- 修改文件：`CustomerServiceScreen.tsx`
- 修改位置：语音消息渲染部分（第662-706行）

#### 3.3 修改图片消息布局
- 从 `imageMessageBubble` 组件中移除 `messageTimeContainer`
- 在图片消息气泡外部添加时间和已读状态显示
- 修改文件：`CustomerServiceScreen.tsx`
- 修改位置：图片消息渲染部分（第708-729行）

#### 3.4 修改文件消息布局
- 从 `fileMessageBubble` 组件中移除 `messageTimeContainer`
- 在文件消息气泡外部添加时间和已读状态显示
- 修改文件：`CustomerServiceScreen.tsx`
- 修改位置：文件消息渲染部分（第731-755行）

### 4. 调整消息气泡样式
- 移除消息气泡内部的底部内边距
- 确保消息气泡样式简洁，只包含文本内容
- 修改文件：`CustomerServiceScreen.tsx`
- 修改位置：`messageBubble`、`voiceMessageBubble`、`imageMessageBubble` 和 `fileMessageBubble` 样式

### 5. 添加外部时间容器样式
- 为消息外部的时间和已读状态添加新的样式
- 确保时间和已读状态正确对齐
- 修改文件：`CustomerServiceScreen.tsx`
- 修改位置：添加新的样式规则

通过以上调整，客服模块将完全符合微信的设计风格，包括更浅的对话窗口底色、圆角正方体头像以及消息外部的时间和已读状态显示。