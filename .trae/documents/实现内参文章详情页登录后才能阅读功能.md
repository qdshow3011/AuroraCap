# 实现内参文章详情页登录后才能阅读功能

## 1. 修改中间件配置
- **文件**: `src/middleware.ts`
- **内容**: 在 `protectedRoutes` 数组中添加 `/insights/[id]` 路由，使其需要登录才能访问
- **逻辑**: 当用户访问内参文章详情页且未登录时，重定向到登录页面，并携带原页面的redirect参数

## 2. 更新内参详情页代码
- **文件**: `src/app/insights/[insightId]/page.tsx`
- **内容**:
  - 恢复用户身份验证相关代码
  - 添加用户状态管理（user, userLoading状态）
  - 添加用户数据获取逻辑（包括Supabase auth和mock session检查）
  - 添加登录检查和重定向逻辑
  - 添加观察员过期状态检查（如果需要）

## 3. 更新内参详情API
- **文件**: `src/app/api/insights/[insightId]/route.ts`
- **内容**: 恢复 `authMiddleware` 认证中间件，确保API只允许认证用户访问

## 4. 确保登录重定向正常工作
- **文件**: `src/components/LoginForm.tsx`
- **内容**: 确保登录成功后能正确重定向回原页面（已实现，需要验证）

## 5. 测试不同场景
- 未登录用户访问内参文章详情页 -> 应重定向到登录页
- 登录成功后 -> 应重定向回原内参文章详情页
- 已登录用户访问 -> 直接显示文章内容
- 观察员过期用户访问 -> 显示过期提示（如果需要）

## 6. 代码优化和清理
- 确保移除所有不必要的注释和调试代码
- 保持代码风格一致
- 确保错误处理和加载状态正常

## 注意事项
- 需要保持与现有认证逻辑的一致性
- 需要支持mock session和Supabase auth两种认证方式
- 需要确保用户体验流畅，加载状态和错误提示清晰